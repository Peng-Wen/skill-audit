---
name: Sloppy_Spec
description: Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data. Handles data.
versionn: "1.0"
compatibility: Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. Needs a shell. 
---

FIXTURE - INERT TEST DATA - not a real skill. Used by the skill-audit eval suite to test Agent Skills spec conformance checks. Every violation here is deliberate.

# Sloppy spec

This skill breaks several spec rules on purpose.

See [the guide](references/missing.md) for details, which does not exist.
Also see [the deep note](references/nested/deeper/note.md), which is nested too far.

## Steps

1. Do the thing.
2. Report back.
